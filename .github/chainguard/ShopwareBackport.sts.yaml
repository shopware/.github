issuer: https://token.actions.githubusercontent.com
subject_pattern: repo:shopware/shopware(-private)?:.*
claim_pattern:
  workflow_ref: shopware/shopware(-private)?/.github/workflows/backport.ya?ml@refs/heads/trunk

permissions:
  contents: write
  pull_requests: write
  workflows: write

repositories:
  - shopware
  - shopware-private
